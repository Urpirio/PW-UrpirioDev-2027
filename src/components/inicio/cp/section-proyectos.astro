---
import CardContents from "../../share-ui/CardContents.astro";
import { data_proyectos } from "../data/data-proyectos";
import { Fetch } from "../../../lib/Fetch-Public-Informacion";
import type { tp_proyectos, tp_section_proyecto } from "../types/tp-proyectos";
import { date } from "astro:schema";

const data: tp_section_proyecto = await Fetch({
  pathname: "/api/section-proyecto?populate=*",
});
---

<section class="px-10 py-20">
  <h2 class="text-center text-4xl font-black text-[#38b000] sm:text-5xl">
    {data.data.titulo}
  </h2>
  <p class="mt-2 text-center text-gray-600">
    {data.data.subtitulo}
  </p>

  <div class="mt-12 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
    <!-- {
      data.map((proyecto) => (
        
      ))
    } -->
    <!-- <CardContents>
          <img
            src={proyecto.images[0].url}
            alt={proyecto.images[0].alt}
            class="h-48 w-full rounded-xl object-cover"
            loading="lazy"
          />

          <div class="mt-4 flex items-center justify-between gap-2">
            <h3 class="text-lg font-bold">{proyecto.name}</h3>
            <span class="text-xs whitespace-nowrap text-gray-500">
              {proyecto.empresa}
            </span>
          </div>

          <p class="mt-2 text-sm text-gray-600">{proyecto.description}</p>

          <div class="mt-4 flex flex-wrap gap-2">
            {proyecto.technology.map((tech) => (
              <span class="rounded-full bg-[#38b000]/10 px-3 py-1 text-xs font-medium text-[#38b000]">
                {tech.name}
              </span>
            ))}
          </div>

          <div class="mt-4 flex gap-3">
            <a
              href={proyecto.url_proyecto}
              target="_blank"
              rel="noreferrer"
              class="rounded-full bg-[#38b000] px-4 py-2 text-sm font-medium text-white transition-colors hover:bg-gray-800"
            >
              Ver proyecto
            </a>
            <a
              href={proyecto.url_githud}
              target="_blank"
              rel="noreferrer"
              class="rounded-full border border-gray-400 px-4 py-2 text-sm font-medium transition-colors hover:border-black"
            >
              GitHub
            </a>
          </div>
        </CardContents> -->
  </div>

  <div class="mt-10 flex justify-center">
    <a
      href="/proyectos"
      class="rounded-full border border-gray-400 px-6 py-3 font-medium transition-colors hover:border-black"
    >
      Ver más
    </a>
  </div>
</section>
